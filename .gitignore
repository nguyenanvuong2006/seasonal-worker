# dependencies
/node_modules
/worker/node_modules

# next.js
/.next/
/out/
/build

# env files (never commit secrets)
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / OS
.DS_Store
*.pem

# local object-storage (dev/test) — production dùng GCS/S3
.storage/

# Local HTML/PDF visual-verification evidence (PDFs and screenshots are retained
# in the operator/CI artifact store, never committed to application source).
/artifacts/
